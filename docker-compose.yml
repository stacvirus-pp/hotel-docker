services:
  keycloakdb:
    image: postgres:15
    container_name: keycloakdb
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: password
    ports:
      - 5000:5432
    networks:
      - app-network

  keycloak_web:
    build:
      context: ../hotel-keycloak
    container_name: keycloak_web
    environment:
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloakdb:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: password

      KC_HOSTNAME: localhost
      KC_HOSTNAME_PORT: 8080
      KC_HOSTNAME_STRICT: false
      KC_HOSTNAME_STRICT_HTTPS: false

      KC_LOG_LEVEL: info
      KC_METRICS_ENABLED: true
      KC_HEALTH_ENABLED: true
      KC_BOOTSTRAP_ADMIN_USERNAME: admin
      KC_BOOTSTRAP_ADMIN_PASSWORD: admin
      KC_FEATURES: preview,token-exchange,admin-fine-grained-authz
    command: start-dev --import-realm
    depends_on:
      - keycloakdb
    ports:
      - "8080:8080"
    networks:
      - app-network

  hotel_managementdb:
    image: postgres:15
    container_name: hotel_managementdb
    volumes:
      - hotel_management_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: hotel
      POSTGRES_USER: hotel
      POSTGRES_PASSWORD: password
    ports:
      - 5001:5432
    networks:
      - app-network

  minio:
    image: minio/minio:RELEASE.2023-11-01T18-37-25Z
    container_name: minio
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
    command: server /data --console-address ":9001"
    networks:
      - app-network
    volumes:
      - minio_new_volume:/data

volumes:
  postgres_data:
  hotel_management_data:
  minio_data:
  minio_new_volume:

networks:
  app-network:
    driver: bridge